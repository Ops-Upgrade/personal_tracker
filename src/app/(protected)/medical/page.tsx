import MedicalView from "@/components/medical/MedicalView";

export const metadata = {
  title: "Medical Records — Ops Upgrade",
  description: "Track medical visits, diagnoses, and reports.",
};

export default function MedicalPage() {
  return <MedicalView />;
}
